# Expense-Tracker

MediChat / Expense-Tracker — a Streamlit-based RAG medical/expense document chatbot.

This repository contains the Streamlit app, utilities for PDF extraction, vector store
creation, and chat integration. Use the `environment.yml` to create a reproducible
conda environment (includes `pyarrow`, `ffmpeg`, and other binary dependencies).

See `requirements.txt` and `environment.yml` for setup instructions.
